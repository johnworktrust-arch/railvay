limit_req_zone $binary_remote_addr zone=ceavpn_sub:10m rate=3r/s;

server {
    listen 80 default_server;
    listen [::]:80 default_server;
    server_name sub.79-137-197-51.sslip.io cover.79-137-197-51.sslip.io;

    location ^~ /.well-known/acme-challenge/ {
        root /var/www/letsencrypt;
        try_files $uri =404;
    }

    location / {
        return 404;
    }
}

server {
    listen 8443 ssl;
    listen [::]:8443 ssl;
    server_name sub.79-137-197-51.sslip.io;

    ssl_certificate /etc/letsencrypt/live/sub.79-137-197-51.sslip.io/fullchain.pem;
    ssl_certificate_key /etc/letsencrypt/live/sub.79-137-197-51.sslip.io/privkey.pem;
    ssl_protocols TLSv1.2 TLSv1.3;
    ssl_session_cache shared:ceavpn_tls:10m;
    ssl_session_timeout 1d;
    server_tokens off;

    # TLS fallback for networks where REALITY on 443 is unavailable. The
    # renderer replaces this exact, root-only path together with Xray's
    # loopback WebSocket path. Never enable a broad /ws location.
    location = __FALLBACK_WS_PATH__ {
        access_log off;
        error_log /dev/null;
        proxy_pass http://127.0.0.1:10001;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection "upgrade";
        proxy_set_header Host $host;
        proxy_connect_timeout 5s;
        proxy_read_timeout 1h;
        proxy_send_timeout 1h;
        proxy_buffering off;
    }

    location ~ "^/happ/([A-Za-z0-9._~-]{1,160})/?$" {
        access_log off;
        default_type text/html;
        add_header Cache-Control "no-store" always;
        add_header Referrer-Policy "no-referrer" always;
        add_header X-Content-Type-Options "nosniff" always;
        add_header X-Robots-Tag "noindex, nofollow, noarchive" always;
        add_header Content-Security-Policy "default-src 'none'; script-src 'unsafe-inline'; style-src 'unsafe-inline'; base-uri 'none'; frame-ancestors 'none'" always;
        return 200 '<!doctype html><html lang="ru"><head><meta charset="utf-8"><meta name="viewport" content="width=device-width,initial-scale=1"><meta http-equiv="refresh" content="0;url=happ://add/https://sub.79-137-197-51.sslip.io:8443/sub/$1"><title>Открываем Happ</title><style>body{font-family:system-ui,sans-serif;padding:32px;max-width:520px;margin:auto;color:#172033}a{display:inline-block;padding:14px 20px;border-radius:12px;background:#2481cc;color:white;text-decoration:none;font-weight:700}</style></head><body><h1>Открываем Happ…</h1><p>Если приложение не открылось автоматически, нажмите кнопку.</p><p><a href="happ://add/https://sub.79-137-197-51.sslip.io:8443/sub/$1">Открыть в Happ</a></p><script>window.location.replace("happ://add/https://sub.79-137-197-51.sslip.io:8443/sub/$1");</script></body></html>';
    }

    location ~ "^/v2box/([A-Za-z0-9._~-]{1,160})/?$" {
        access_log off;
        default_type text/html;
        add_header Cache-Control "no-store" always;
        add_header Referrer-Policy "no-referrer" always;
        add_header X-Content-Type-Options "nosniff" always;
        add_header X-Robots-Tag "noindex, nofollow, noarchive" always;
        add_header Content-Security-Policy "default-src 'none'; script-src 'unsafe-inline'; style-src 'unsafe-inline'; base-uri 'none'; frame-ancestors 'none'" always;
        return 200 '<!doctype html><html lang="ru"><head><meta charset="utf-8"><meta name="viewport" content="width=device-width,initial-scale=1"><meta http-equiv="refresh" content="0;url=v2box://install-sub?url=https%3A%2F%2Fsub.79-137-197-51.sslip.io%3A8443%2Fsub%2F$1&amp;name=CEA%20VPN"><title>Открываем V2Box</title><style>body{font-family:system-ui,sans-serif;padding:32px;max-width:520px;margin:auto;color:#172033}a{display:inline-block;padding:14px 20px;border-radius:12px;background:#2481cc;color:white;text-decoration:none;font-weight:700}.store{background:#334155}</style></head><body><h1>Открываем V2Box…</h1><p>Если приложение не открылось автоматически, нажмите кнопку.</p><p><a href="v2box://install-sub?url=https%3A%2F%2Fsub.79-137-197-51.sslip.io%3A8443%2Fsub%2F$1&amp;name=CEA%20VPN">Открыть в V2Box</a></p><p><a class="store" href="https://apps.apple.com/app/v2box-v2ray-client/id6446814690">Установить V2Box</a></p><script>window.location.replace("v2box://install-sub?url=https%3A%2F%2Fsub.79-137-197-51.sslip.io%3A8443%2Fsub%2F$1&name=CEA%20VPN");</script></body></html>';
    }

    location ^~ /sub/ {
        access_log off;
        limit_req zone=ceavpn_sub burst=10 nodelay;
        # Happ's ping can succeed even when a stale routing profile keeps
        # device traffic outside the tunnel. The documented subscription
        # header disables client-side routing and restores a full tunnel.
        add_header routing-enable "0" always;
        # Force the raw subscription format even when Telegram opens this URL
        # in a browser. Otherwise Marzban renders an HTML page with a direct
        # VLESS link, which Happ imports as a non-updatable standalone server.
        proxy_set_header Accept "text/plain";
        proxy_pass http://127.0.0.1:8000;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto https;
        proxy_connect_timeout 5s;
        proxy_read_timeout 15s;
    }

    location / {
        return 404;
    }
}

server {
    listen 127.0.0.1:9443 ssl;
    server_name cover.79-137-197-51.sslip.io;

    ssl_certificate /etc/letsencrypt/live/sub.79-137-197-51.sslip.io/fullchain.pem;
    ssl_certificate_key /etc/letsencrypt/live/sub.79-137-197-51.sslip.io/privkey.pem;
    ssl_protocols TLSv1.2 TLSv1.3;
    access_log off;
    server_tokens off;
    default_type text/html;

    return 200 '<!doctype html><html><head><title>CEA VPN</title></head><body></body></html>';
}
