# CeaAI MVP Documentation

## Быстрый запуск технического MVP

В этом репозитории реализован Telegram-бот CeaAI с SQLite/Postgres, mock-платежами и mock AI-провайдерами. Реальные AI API и реальная платежка не подключаются.

### Что внутри

- `ceai/bot` — команды, меню и callback handlers Telegram-бота.
- `ceai/services` — бизнес-логика пользователей, подписок, платежей, coins и генераций.
- `ceai/repositories` — доступ к SQLite-таблицам из модели данных.
- `ceai/providers/mock.py` — заглушки text/image/video/tts.
- `migrations/001_init.sql` — схема БД из `Документация.md`.
- `migrations/postgres/001_init.sql` — та же схема для Postgres.
- `ceai/seed.py` — стартовые тарифы и цены моделей.
- `tests/test_business_logic.py` — минимальная проверка ключевых правил.
- `Dockerfile` — production-образ для Northflank/Docker-хостинга.

### Локальный запуск

```bash
python3 -m venv .venv
source .venv/bin/activate
pip install -e .
cp .env.example .env
```

В `.env` нужно указать токен Telegram-бота:

```bash
TELEGRAM_BOT_TOKEN=put-your-telegram-bot-token-here
DATABASE_URL=sqlite:///./data/ceai.sqlite3
MOCK_PAYMENT_BASE_URL=https://mock-payments.local/pay
```

Создать БД и заполнить тарифы/модели можно отдельно:

```bash
python -m ceai.seed
```

Запуск бота:

```bash
python -m ceai.main
```

При старте бот также автоматически применяет миграции и seed-данные.

### Деплой на Northflank

Текущая production-схема рассчитана на polling-бота и Postgres.

1. Создайте проект в Northflank.
2. Создайте бесплатную Postgres database.
3. Создайте service из GitHub-репозитория проекта.
4. Выберите Dockerfile build.
5. Укажите переменные окружения:

```bash
TELEGRAM_BOT_TOKEN=your-botfather-token
DATABASE_URL=postgresql://user:password@host:5432/dbname
APP_ENV=production
MOCK_PAYMENT_BASE_URL=https://mock-payments.local/pay
```

6. Healthcheck:

```text
Path: /healthz
Port: $PORT
```

Northflank передает `PORT` автоматически. Если переменная `PORT` задана, приложение параллельно с Telegram polling поднимает HTTP endpoint `/healthz`.

Важно: на бесплатном плане Northflank может попросить добавить банковскую карту для верификации даже без списаний. Если карта не подходит, используйте Render fallback ниже.

### Бесплатный fallback на Render

В репозитории есть `render.yaml` для Blueprint-деплоя:

- free Docker web service;
- free Render Postgres;
- healthcheck `/healthz`;
- Telegram webhook `/telegram/webhook`;
- `TELEGRAM_BOT_TOKEN` вводится вручную в Render как secret.

Render-деплой использует webhook-режим, потому что polling-бот на Render Free засыпает и не просыпается от Telegram-сообщений.

Ограничения Render free:

- web service может засыпать после периода без входящих HTTP-запросов;
- первый ответ после сна может быть медленным, но Telegram webhook сможет разбудить сервис;
- free Postgres подходит для MVP/демо, но не для продакшена.

Шаги:

1. Откройте Render Dashboard.
2. Создайте Blueprint из GitHub-репозитория.
3. Выберите этот repo.
4. Укажите `TELEGRAM_BOT_TOKEN` в env.
5. После деплоя проверьте `/healthz` и Telegram `/start`.

### Проверка сценария в Telegram

```text
/start
-> Тарифы
-> выбрать Старт / Базовый / Про
-> Оплатить тестово
-> AI-инструменты
-> выбрать модель
-> отправить prompt
-> получить mock result
-> Баланс / История
```

Чтобы проверить возврат coins при ошибке AI-провайдера, выберите модель и отправьте prompt `mock_error`.

### Тесты бизнес-логики

```bash
python -m unittest discover -s tests -v
```

Проверяется:

- успешная тестовая оплата начисляет coins один раз;
- повторный webhook не начисляет coins второй раз;
- успешная генерация списывает coins;
- ошибка генерации возвращает coins;
- нельзя генерировать без активной подписки;
- нельзя генерировать при нехватке coins.

## Суть за 1 минуту

CeaAI - это Telegram-бот, где пользователь покупает подписку, получает коины и тратит их на разные AI-инструменты: чат с нейросетями, генерацию картинок, видео, музыки и контента.

MVP должен проверить не "можем ли мы подключить много нейросетей", а более важный вопрос: готовы ли пользователи платить за удобный AI-агрегатор прямо в Telegram.

Базовая логика:

```text
Пользователь покупает подписку
    -> получает коины
    -> выбирает AI-инструмент
    -> отправляет запрос
    -> backend проверяет баланс
    -> вызывает нужную нейросеть
    -> бот отправляет результат
```

Главные части MVP:

- Telegram-бот как основной интерфейс;
- backend, где живет вся бизнес-логика;
- база данных для пользователей, подписок, платежей и генераций;
- внешняя платежная система со ссылкой на оплату;
- внутренняя валюта coins;
- адаптеры для подключения разных AI-провайдеров.

## 1. Что такое CeaAI

CeaAI - это Telegram-бот-агрегатор нейросетей. Пользователь покупает подписку, получает внутренние коины/лимиты и тратит их на разные AI-инструменты:

- общение с нейросетями: ChatGPT, Claude, DeepSeek и другие;
- генерация изображений;
- генерация видео;
- генерация музыки;
- обработка и улучшение текста;
- будущие инструменты для маркетинга, SEO, контента и бизнеса.

Главная идея продукта: пользователь не разбирается в разных AI-сервисах, API, VPN, оплатах и подписках. Он открывает одного Telegram-бота, выбирает задачу, отправляет запрос и получает результат.

## 2. Формат продукта

На старте CeaAI делается именно как Telegram-бот, а не как Mini App.

Почему бот:

- быстрее сделать MVP;
- пользователю привычнее писать запросы в чат;
- проще отправлять текст, фото, видео и документы;
- легче тестировать спрос;
- меньше затрат на frontend.

Mini App можно добавить позже как дополнительный интерфейс для:

- красивой галереи генераций;
- истории работ;
- личного кабинета;
- управления подпиской;
- сложных настроек генерации.

## 3. Основной пользовательский сценарий

1. Пользователь открывает Telegram-бота.
2. Нажимает `Start`.
3. Бот показывает приветствие и главное меню.
4. Пользователь видит:
   - баланс коинов;
   - активную подписку;
   - доступные AI-инструменты;
   - кнопку пополнения/покупки подписки.
5. Пользователь покупает подписку через внешнюю платежную ссылку.
6. После успешной оплаты бот начисляет коины.
7. Пользователь выбирает нейросеть или задачу.
8. Бот показывает стоимость действия в коинах.
9. Пользователь отправляет запрос.
10. Backend проверяет подписку и баланс.
11. Backend отправляет запрос в нужный AI-провайдер.
12. Пользователь получает результат в Telegram.
13. С баланса списываются коины.

## 4. Подписки и коины

В CeaAI используется гибридная модель:

- пользователь покупает подписку;
- подписка выдает определенное количество коинов на период;
- коины тратятся на AI-действия;
- если коины закончились, пользователь может докупить пакет коинов или перейти на тариф выше.

Коины нужны, чтобы:

- гибко оценивать разные нейросети;
- не объяснять пользователю себестоимость API;
- контролировать расходы;
- менять цены моделей без изменения логики подписок.

## 5. Черновая тарифная сетка

Точные лимиты нужно будет пересчитать после выбора моделей и понимания себестоимости API.

### Start

Пример: 300 рублей в месяц.

Для пользователей, которые хотят попробовать сервис.

Может включать:

- небольшой пакет коинов;
- доступ к текстовым нейросетям;
- ограниченное количество генераций изображений;
- без приоритета в очереди.

### Plus

Пример: 800 рублей в месяц.

Основной тариф, который должен быть самым выгодным и популярным.

Может включать:

- больше коинов;
- общение с ChatGPT, Claude, DeepSeek;
- генерацию изображений;
- ограниченную генерацию видео/музыки;
- историю генераций.

### Pro

Пример: 3000 рублей за 2 месяца.

Тариф для активных пользователей, создателей контента, маркетологов и владельцев небольшого бизнеса.

Может включать:

- большой пакет коинов;
- доступ к дорогим моделям;
- больше генераций видео и музыки;
- приоритетную очередь;
- расширенную историю;
- доступ к SEO/контент-инструментам.

### SEO Pro

Отдельный тариф или режим внутри Pro, если продукт будет развиваться в сторону SEO и контент-маркетинга.

Может включать:

- генерацию статей;
- контент-планы;
- кластеризацию ключевых слов;
- SEO-заголовки и описания;
- анализ конкурентов;
- массовую генерацию текстов.

## 6. Пример стоимости действий в коинах

Цены ниже являются примером. Их нужно будет настроить после расчета себестоимости.

| Действие | Примерная цена |
| --- | ---: |
| Сообщение в DeepSeek | 1 coin |
| Сообщение в ChatGPT | 2 coins |
| Сообщение в Claude | 2 coins |
| Генерация изображения | 5 coins |
| Улучшение изображения | 4 coins |
| Генерация короткого видео | 25 coins |
| Генерация музыки | 20 coins |
| SEO-статья | 10 coins |

Важно: пользователь должен видеть не только коины, но и понятное объяснение.

Пример:

`800 рублей = 500 коинов. Этого хватит примерно на 200 сообщений, или 80 изображений, или 15 коротких видео.`

## 7. Архитектура MVP

Общая схема:

```text
Telegram Bot
    |
    v
CeaAI Backend
    |
    +--> Database
    |
    +--> Payment Provider
    |
    +--> AI Provider Adapters
    |
    +--> File Storage
```

### Telegram Bot

Отвечает за интерфейс:

- главное меню;
- выбор тарифа;
- покупку подписки;
- выбор нейросети;
- прием запросов пользователя;
- отправку результатов;
- уведомления о балансе и ошибках.

Бот не должен напрямую работать с платежками и AI API. Он общается с backend.

### Backend

Главный сервер CeaAI.

Отвечает за:

- пользователей;
- подписки;
- баланс коинов;
- списания и начисления;
- платежные webhook;
- вызовы AI-провайдеров;
- историю генераций;
- обработку ошибок;
- антифрод и лимиты.

### Database

Хранит:

- пользователей;
- подписки;
- тарифы;
- баланс коинов;
- операции с коинами;
- платежи;
- AI-запросы;
- историю генераций;
- настройки моделей.

Для MVP можно использовать SQLite или PostgreSQL. Для продакшена лучше PostgreSQL.

### Payment Provider

Основной сценарий оплаты:

1. Пользователь выбирает тариф.
2. Backend создает платеж.
3. Платежная система возвращает ссылку.
4. Бот отправляет пользователю кнопку `Оплатить`.
5. Пользователь оплачивает на странице платежки.
6. Платежка отправляет webhook на backend.
7. Backend проверяет подпись webhook.
8. Backend активирует подписку и начисляет коины.
9. Бот уведомляет пользователя об успешной оплате.

Возможные платежки:

- ЮKassa;
- CloudPayments;
- Robokassa;
- Т-Банк Касса.

Важно: начислять коины можно только после подтвержденного webhook от платежной системы.

### AI Provider Adapters

Каждая нейросеть подключается через отдельный адаптер.

Пример:

```text
providers/
  openai
  anthropic
  deepseek
  xai
  image
  video
  music
```

Backend должен иметь единый внутренний интерфейс:

```text
generateText(model, prompt, userContext)
generateImage(model, prompt, options)
generateVideo(model, prompt, options)
generateMusic(model, prompt, options)
```

Так бот не зависит от конкретного API. Если одна модель меняется или отключается, остальная система продолжает работать.

## 8. Как подключаются нейросети

Для каждой нейросети нужны:

- аккаунт разработчика;
- API key;
- понимание тарифов API;
- лимиты;
- формат запроса;
- формат ответа;
- обработка ошибок.

Пример логики:

1. Пользователь выбирает `ChatGPT`.
2. Backend смотрит цену модели в коинах.
3. Backend проверяет баланс.
4. Backend резервирует коины.
5. Backend отправляет запрос в OpenAI API.
6. Если ответ успешный, backend списывает коины окончательно.
7. Если произошла ошибка, backend возвращает коины.
8. Бот отправляет ответ пользователю.

Для долгих задач, например видео и музыки, нужна очередь задач.

Пример:

```text
User request
    |
    v
Create generation job
    |
    v
Reserve coins
    |
    v
Send job to video/music provider
    |
    v
Wait via polling or webhook
    |
    v
Save result
    |
    v
Send result to Telegram
```

## 9. Что входит в MVP

MVP должен проверить главный вопрос: готовы ли пользователи платить за удобный AI-агрегатор в Telegram.

Минимальный набор:

- Telegram-бот;
- регистрация пользователя после `/start`;
- главное меню;
- баланс коинов;
- 3 тарифа;
- создание платежной ссылки;
- webhook успешной оплаты;
- начисление коинов;
- текстовая нейросеть;
- генерация изображений;
- история последних генераций;
- админская настройка цен хотя бы через конфиг;
- базовые логи ошибок.

В MVP можно не делать:

- сложную Mini App;
- полноценную админ-панель;
- десятки нейросетей;
- реферальную систему;
- командные аккаунты;
- сложную аналитику;
- SEO Pro-инструменты.

## 10. Главное меню бота

Пример структуры:

```text
Главное меню

Баланс: 420 coins
Подписка: Plus до 15.07.2026

[Нейросети]
[Генерация изображений]
[Видео]
[Музыка]
[Мой баланс]
[Тарифы]
[История]
[Поддержка]
```

## 11. Сценарий покупки подписки

```text
Пользователь -> Тарифы
Бот -> показывает Start / Plus / Pro
Пользователь -> выбирает Plus
Backend -> создает payment pending
Payment Provider -> возвращает payment_url
Бот -> отправляет кнопку оплаты
Пользователь -> оплачивает
Payment Provider -> webhook paid
Backend -> проверяет webhook
Backend -> активирует подписку
Backend -> начисляет coins
Бот -> "Оплата прошла. Вам начислено 500 coins."
```

## 12. Сценарий генерации текста

```text
Пользователь -> Нейросети
Пользователь -> ChatGPT
Бот -> "Отправьте запрос. Стоимость: 2 coins."
Пользователь -> пишет запрос
Backend -> проверяет подписку
Backend -> проверяет баланс
Backend -> отправляет запрос в AI API
AI Provider -> возвращает ответ
Backend -> списывает coins
Backend -> сохраняет историю
Бот -> отправляет ответ
```

## 13. Сценарий генерации изображения

```text
Пользователь -> Генерация изображений
Бот -> "Опишите картинку. Стоимость: 5 coins."
Пользователь -> пишет промпт
Backend -> проверяет баланс
Backend -> резервирует coins
Backend -> отправляет запрос в image API
AI Provider -> возвращает image_url или файл
Backend -> сохраняет результат
Backend -> списывает coins
Бот -> отправляет изображение
```

## 14. Основные таблицы базы данных

### users

Пользователи Telegram.

Поля:

- id;
- telegram_id;
- username;
- first_name;
- created_at;
- last_seen_at;
- status.

### plans

Тарифы.

Поля:

- id;
- code;
- name;
- price_rub;
- duration_days;
- coins_amount;
- is_active.

### subscriptions

Активные подписки пользователей.

Поля:

- id;
- user_id;
- plan_id;
- starts_at;
- ends_at;
- status.

### coin_transactions

Все движения коинов.

Поля:

- id;
- user_id;
- amount;
- type;
- reason;
- related_entity_id;
- created_at.

Типы:

- payment_bonus;
- generation_charge;
- refund;
- manual_adjustment;
- subscription_grant.

### payments

Платежи.

Поля:

- id;
- user_id;
- provider;
- provider_payment_id;
- amount_rub;
- status;
- payment_url;
- plan_id;
- created_at;
- paid_at.

### ai_models

Настройки моделей.

Поля:

- id;
- provider;
- model_code;
- display_name;
- type;
- coin_price;
- is_active.

Типы:

- text;
- image;
- video;
- music.

### generations

История генераций.

Поля:

- id;
- user_id;
- model_id;
- type;
- prompt;
- result_url;
- result_text;
- status;
- coins_spent;
- error_message;
- created_at;
- completed_at.

## 15. Правила списания коинов

Для быстрых задач:

1. Проверить баланс.
2. Выполнить запрос.
3. Списать коины после успешного результата.

Для дорогих или долгих задач:

1. Проверить баланс.
2. Зарезервировать коины.
3. Запустить генерацию.
4. Если успешно - списать резерв.
5. Если ошибка - вернуть резерв.

Это важно, чтобы пользователь не терял коины из-за ошибок провайдера.

## 16. Обработка ошибок

Примеры ошибок:

- недостаточно коинов;
- подписка закончилась;
- AI-провайдер недоступен;
- платеж не подтвержден;
- пользователь отправил неподдерживаемый файл;
- генерация заняла слишком много времени.

Бот должен отвечать простым языком.

Пример:

`Не получилось сгенерировать изображение. Коины не списаны. Попробуйте еще раз чуть позже.`

## 17. Админ-функции

На старте можно сделать админ-функции через команды в Telegram или через конфиг.

Нужно уметь:

- смотреть пользователя;
- менять баланс вручную;
- включать/выключать модель;
- менять стоимость модели;
- смотреть последние платежи;
- смотреть ошибки генераций;
- выдавать промокод или бонус.

Позже можно сделать отдельную веб-админку.

## 18. Метрики MVP

Важно отслеживать:

- сколько пользователей нажали `/start`;
- сколько пользователей попробовали бесплатные коины;
- сколько дошли до оплаты;
- конверсия в оплату;
- средний чек;
- какие модели используют чаще;
- сколько стоит один активный пользователь;
- сколько API-расходов на пользователя;
- маржа по тарифам;
- сколько пользователей возвращаются на следующий день.

## 19. Главные риски

### Экономика

Если неправильно посчитать стоимость API, можно продавать подписки дешевле себестоимости.

Решение:

- считать примерную себестоимость каждого действия;
- ставить запас по марже;
- быстро менять цены в коинах.

### Стабильность AI API

Некоторые провайдеры могут тормозить, падать или менять цены.

Решение:

- делать адаптеры;
- логировать ошибки;
- возвращать коины при сбоях;
- иметь альтернативные модели.

### Сложность продукта

Если сразу добавить много нейросетей, продукт станет сложным и неудобным.

Решение:

- начать с небольшого набора популярных сценариев;
- сделать понятное меню;
- добавлять модели по спросу.

## 20. Дорожная карта

### Этап 1: Документация и проектирование

- описать механику продукта;
- определить тарифы;
- определить первые модели;
- выбрать платежку;
- выбрать технический стек.

### Этап 2: MVP бота

- Telegram-бот;
- база пользователей;
- баланс коинов;
- тарифы;
- платежная ссылка;
- webhook оплаты;
- одна текстовая модель;
- одна модель генерации изображений.

### Этап 3: Улучшение продукта

- история генераций;
- реферальная система;
- промокоды;
- больше моделей;
- очередь для видео/музыки;
- админ-функции.

### Этап 4: Масштабирование

- Mini App;
- веб-кабинет;
- SEO Pro-инструменты;
- командные аккаунты;
- аналитика;
- партнерская программа.

## 21. Принцип разработки

CeaAI нужно строить не как набор случайно подключенных API, а как платформу.

Правильная логика:

```text
Telegram - это интерфейс.
Backend - это мозг продукта.
AI-провайдеры - это заменяемые модули.
Коины - это внутренняя экономика.
Подписка - это основная модель монетизации.
```

Такой подход позволит сначала быстро запустить Telegram-бота, а потом без полной переделки добавить Mini App, сайт, админку и новые нейросети.
